import {
  Color,
  Scene,
} from "../../../node_modules/three/build/three.module.js";

function createScene() {
  const scene = new Scene();

  // scene.background = new Color("#FBE5E5");

  return scene;
}

export { createScene };
